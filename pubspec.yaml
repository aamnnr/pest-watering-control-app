name: pestmist_control
description: Aplikasi kontrol pest misting

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.3
  equatable: ^2.0.5
  mqtt_client: ^10.4.0
  flutter_blue_plus: ^1.32.0
  hive_flutter: ^1.1.0
  hive: ^2.2.3
  path_provider: ^2.1.1
  shared_preferences: ^2.2.2
  fl_chart: ^0.66.0
  intl: ^0.18.1
  flutter_local_notifications: ^16.3.0
  permission_handler: ^11.0.1
  json_annotation: ^4.8.1
  get_it: ^7.6.4
  logger: ^1.4.0
  connectivity_plus: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  build_runner: ^2.4.7
  json_serializable: ^6.7.1
  hive_generator: ^2.0.0

flutter:
  uses-material-design: true